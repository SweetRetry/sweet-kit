{
  "name": "server",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "DOTENV_CONFIG_PATH=.env.local tsx watch src/index.ts",
    "start": "DOTENV_CONFIG_PATH=.env.local node dist/index.js",
    "auth:generate": "pnpm --filter @workspace/database build && DOTENV_CONFIG_PATH=.env.local auth generate --config src/auth.ts --output ../../packages/database/src/schema.ts --yes && pnpm exec biome check --write ../../packages/database/src/schema.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@ai-sdk/openai": "^4.0.51",
    "@hono/node-server": "^2.1.1",
    "@hono/otel": "^1.1.2",
    "@hono/zod-openapi": "^1.6.1",
    "@scalar/hono-api-reference": "^0.12.0",
    "@workspace/auth": "workspace:*",
    "@workspace/database": "workspace:*",
    "@workspace/logger": "workspace:*",
    "@workspace/observability": "workspace:*",
    "@workspace/request": "workspace:*",
    "ai": "^7.0.84",
    "dotenv": "^17.4.2",
    "hono": "^4.13.5",
    "hono-pino": "^0.10.3",
    "hono-rate-limiter": "^0.5.3",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "@workspace/jobs": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "auth": "^1.7.2",
    "drizzle-orm": "^0.45.2",
    "graphile-worker": "^0.17.3",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
